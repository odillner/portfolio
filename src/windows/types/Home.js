import React from "react"

import StandardWindow from "../WindowWrapper"

const Home = ({id}) => {
    return (
        <StandardWindow title='Home' id={id}>
        </StandardWindow>
    )
}


export default Home