import React from "react"

import StandardWindow from "../StandardWindow"

const Home = ({id}) => {
    return (
        <StandardWindow title='Home' id={id}>
        </StandardWindow>
    )
}


export default Home